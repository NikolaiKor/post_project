h1 =t('home.tags_cloud.title')
-@tags_cloud.each do |tag|
  p class=tag[:class] =tag[:tag].name
h1 =t('home.last_posts.title')
= render @posts
h1 =t('home.last_events.title')
= render @events
h1 =t('home.last_videos.title')
= render @videos